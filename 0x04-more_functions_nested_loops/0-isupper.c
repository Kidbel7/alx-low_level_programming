#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for upper case character
 * @c: character to be checked
 * Return: 1 or 0
 */

int _isupper(int c)
{
	char c;

	for (c = 'A'; c <= 'Z'; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
